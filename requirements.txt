# Argo — runtime deps  (or: pip install -e .  to also get the `argo` command)
jinja2>=3.1
jsonschema>=4.0
pydantic>=2.0
pyyaml>=6.0
typer>=0.12

# HTTP API (server/) — the backend for the web UI
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9   # repo .zip upload (FastAPI File/UploadFile)

# dev / test
pytest>=8.0
httpx>=0.27
